#include <xc.h>

// Intentionally empty.
//
// Configuration bits are defined in mcc_generated_files/mcc.c (MCC output).
// Keeping this file avoids churn in build scripts while preventing duplicate
// config-word definitions.
